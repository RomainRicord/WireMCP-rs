[package]
name = "capture-packets"
version = "1.0.0"
edition = "2021"

[dependencies]
pcap = "2"
clap = { version = "4", features = ["derive"] }

[profile.release]
opt-level = 3
lto = true
strip = true
